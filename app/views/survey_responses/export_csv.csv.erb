<% @field_values.each do |response| -%>
<% response.each_with_index do |val, index| -%>
<% if index < response.count-1 %>
"<%= val.gsub("\n\r",",").gsub("\r",",").gsub(',,',',') -%>",
<% else %>
"<%= val.gsub("\n\r",",").gsub("\r",",").gsub(',,',',') -%>"
<% end %>
<% end %>
<% end -%>