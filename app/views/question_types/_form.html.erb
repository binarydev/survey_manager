<%= simple_form_for @question_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :type_name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                question_types_path, :class => 'btn' %>
  </div>
<% end %>
