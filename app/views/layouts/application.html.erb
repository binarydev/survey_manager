<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Survey Manager" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <!-- <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %> -->
  </head>
  <body class="<%= @hide_layout.nil? ? '' : 'hidden-layout' %>">

    <div class="navbar <%= @hide_layout.nil? ? '' : 'hidden' %>">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Survey Manager</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Surveys", "/surveys"  %></li>
              <li><%= link_to "Users", "/users"  %></li>
              <li><%= link_to "Generated Reports", "/reports"  %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="<%= @hide_layout.nil? ? 'span9' : 'span12' %>">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
          <div class="<%= @hide_layout.nil? ? 'span3' : 'hidden' %>">
            <div class="well sidebar-nav">
              <h3>Navigation</h3>
              <ul class="nav nav-list">
                <li class="nav-header">Sections</li>
                <li><%= link_to "Surveys", "/surveys"  %></li>
                <li><%= link_to "Users", "/users"  %></li>
                <li><%= link_to "Generated Reports", "/reports"  %></li>
              </ul>
            </div><!--/.well -->
          </div><!--/span-->
        </div><!--/row-->

      <footer class="<%= @hide_layout.nil? ? '' : 'hidden' %>">
        <p>&copy; Survey Manager 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
