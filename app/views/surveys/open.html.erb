<%- model_class = Survey -%>
<div class="page-header">
  <h1><%= @survey.survey_name %></h1>
</div>

<div>
  <%= @survey.intro_text %>
</div>

<%= link_to 'Submit',
            survey_submit_response_path(@survey),
            :class => 'btn btn-primary' %>
